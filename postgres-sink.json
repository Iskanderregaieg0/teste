{
  "name": "postgres-sink",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "userprofile_dim_secteur.public.dim_secteur",
    "connection.url": "jdbc:postgresql://postgres:32050/datawarehouse_db?user=postgres&password=postgres",
    "auto.create": "true",
    "insert.mode": "insert"
  }
}
